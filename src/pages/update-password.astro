---
import PublicPageLayout from "@/layouts/PublicPageLayout.astro";
import { ResetPasswordForm } from "@/components/auth/ResetPasswordForm";

export const prerender = false;
---

<PublicPageLayout title="Set new password · StashTracker">
  <Fragment slot="hero">
    <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary/70">Secure your account</p>
    <h1 class="text-4xl font-semibold tracking-tight text-foreground md:text-5xl">Set a new password</h1>
    <p class="text-base text-muted-foreground">
      Enter your new password below to finish resetting access to your account. You&apos;ll be redirected to sign in
      when you&apos;re done.
    </p>
  </Fragment>

  <ResetPasswordForm client:only="react" />
</PublicPageLayout>
